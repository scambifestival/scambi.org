---
pagination:
  data: 2021-pinoli
  size: 1
permalink: '/2021/pinolo/{{ pagination.pageNumber | plus: 1 }}/'
layout: wrapper
---
{% for item in pagination.items %}
	<h1>{{ item.title }}</h1>
	<section class='row'>
		<div class='half column'>
			<p>{{ item.description | markdownify }}</p>
		</div>
		<div class='half column'>
			<dl id='data'>
				<dt>Location</dt>
				<dd>{{ item.location }} — <a href='{{ site.map }}' target='_blank' title='Le location di Scambi Festival 2021'>vedi mappa</a></dd>

				<dt>Host</dt>
				{% if item.host != '""' %}
					{% assign hosts = item.host | split: ',' %}
					{% for host in hosts %}
						<dd>{{ host }}</dd>
					{% endfor %}
				{% else %}
					<dd>{{ item.host_name }}</dd>
				{% endif %}
			</dl>
		</div>
	</section>
{% endfor %}

<h3>Non serve prenotare i pinoli!</h3>
<p class='center'>Girate per la Pigna e li troverete.</p>